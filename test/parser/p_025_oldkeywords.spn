// 'const', 'global', 'function' and 'argv' are no longer keywords
// so they can be used as variable names
let const = "but please don't do this anyway";
let var = "and not this one either";
let global = "that's slightly better";
let function = fn () { return 1337; };
let argv = [ "fake", "argv", 42 ];

extern const = "why would you call a global 'const'?";
extern var = "or 'var', for that matter?";
extern global = "welcome to the tautology club";
extern function = fn () { return 13 + 37; };
extern argv = fn { "look, not even an array"; };
